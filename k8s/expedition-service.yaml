apiVersion: apps/v1
kind: Deployment
metadata:
  name: expedition-service
  namespace: redbus
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: expedition-service
  template:
    metadata:
      labels:
        app: expedition-service
    spec:
      containers:
      - name: expedition-service
        image: bhavy0949/redbus-expedition-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8082
        env:
        - name: SPRING_APPLICATION_NAME
          value: expedition-service
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://postgres:5432/expeditionDB
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            configMapKeyRef:
              name: redbus-config
              key: POSTGRES_USER
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redbus-secrets
              key: POSTGRES_PASSWORD
        - name: LOGGING_LEVEL_ROOT
          value: "INFO"
        startupProbe:
          tcpSocket:
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 60
        livenessProbe:
          tcpSocket:
            port: 8082
          initialDelaySeconds: 180
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 6
        readinessProbe:
          tcpSocket:
            port: 8082
          initialDelaySeconds: 120
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 6
        resources:
          requests:
            cpu: 250m
            memory: 512Mi
          limits:
            cpu: 600m
            memory: 1Gi
      imagePullSecrets:
      - name: docker-credentials
---
apiVersion: v1
kind: Service
metadata:
  name: expedition-service
  namespace: redbus
spec:
  selector:
    app: expedition-service
  ports:
  - protocol: TCP
    port: 8082
    targetPort: 8082
  type: ClusterIP
